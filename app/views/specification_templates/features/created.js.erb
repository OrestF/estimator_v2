resetForm()
appendEstimationTask()

function resetForm() {
    $(".features-pseudo-table .create_form")[0].reset()
}

function appendEstimationTask() {
    $( "<%=j render(partial: 'specification_templates/features/item_form', locals: { feature: feature, url: update_feature_specification_template_path(specification) })  %>" ).insertBefore( ".features-pseudo-table form:last-of-type" );
}
