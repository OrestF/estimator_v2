alertify.set('notifier','position', 'top-right');
alertify.notify("<%= message %>", '<%= status %>', 7);

<% if url.present? %>
    function redirect() {
      window.location = '<%= url %>'
    }
    setTimeout(redirect, 2000);
<% end %>
