alertify.set('notifier','position', 'top-right');
alertify.notify("success", 'success', 5);

setTotals()

function setTotals() {
    $('#total_optimistic').html('<%= totals[:optimistic] %>')
    $('#total_pessimistic').html('<%= totals[:pessimistic] %>')
}
